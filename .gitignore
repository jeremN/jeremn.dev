node_modules
.DS_Store
dist
dist-ssr
.astro
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# Local dev / secrets
.dev.vars
local.db
local.db-*

# Test artifacts
test-results
playwright-report
/playwright/.cache

# Local Claude Code / agent skill installs — tooling, not part of the site
.claude/skills/
.agents/skills/
skills-lock.json

# Untracked design/spec/plan docs — never commit (project preference)
docs/superpowers/
design/
design.html
.playwright-mcp/

# Scaffold leftovers
.cta.json
.vscode/

# Subagent-driven-development scratch (progress ledger, briefs, review packages)
.superpowers/
